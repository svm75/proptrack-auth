.claude/settings.local.json

# Migration data (real Dataverse export/transform output) — never commit
migration/export/data/
migration/transform/data/
migration/load/generated/
migration/reports/

# Logs
logs
*.log
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# NAS-native document storage — local dev/test scratch root (DOCUMENTS_ROOT), stands in for the
# NAS bind-mount in production. Never commit uploaded documents.
api/data/
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
